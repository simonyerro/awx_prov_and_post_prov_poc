- name: artifact integration test results to the web
  local_action: 'shell ls'
  register: result

- name: artifact URL of test results to Tower Workflows
  set_stats:
    data:
      facts_to_gather:  "{{ result.stdout }}"
